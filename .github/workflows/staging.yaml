name: Deploy (staging)

on:
  push:
    branches:
      - staging

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ vars.FTP_SERVER_STAGING }}
        username: ${{ vars.FTP_USERNAME_STAGING }}
        password: ${{ secrets.FTP_PASSWORD_STAGING }}
        protocol: ftps
        port: 21
        server-dir: /public/

